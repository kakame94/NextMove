// Smoke test placeholder — full integration tests for KlarisApp require
// Supabase + Firebase init, which run in `integration_test/` instead of here.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    expect(1 + 1, 2);
  });
}
